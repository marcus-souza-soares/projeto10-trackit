import { Loader, ThreeDots } from 'react-loader-spinner'

export default function Loader(){
    return(
        <ThreeDots
        color='blue'
        height={100}
        width={100}>

        </ThreeDots>

    )
}